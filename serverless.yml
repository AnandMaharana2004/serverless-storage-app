org: anandmaharana
service: storageapp-serverless

provider:
  name: aws
  runtime: nodejs20.x
  region: ap-south-1

plugins:
  - serverless-offline

functions:
  health:
    handler: src/functions/health/index.handler
    events:
      - httpApi:
          path: /health
          method: get
